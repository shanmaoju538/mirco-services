spring:
  application:
    name: hystrix-dashboard
eureka:
  server:
    enable-self-preservation: false
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/
feign:
  hystrix:
    enabled: true